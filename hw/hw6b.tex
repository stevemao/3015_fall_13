\documentclass[11pt]{article}

\include{header}

\begin{document}
\homework{6}{14 September}


% \section{Type Inference - Table Based Algorithm Extended}


% We extend the methods described in HW5 to include Haskell expressions
% containing pairs (having the product type) and lambdas (which are function
% types.)

% \subsection{Pair expressions and product types}

% Consider the function 
% \[{\mathit{swap}} (x,y) = (y,x)\]

% To create the initial table, on the left we put the pair argument above the
% line choosing new type variables \sfa~and \sfb~and, since the arument $(x,y)$
% is a pair, combine them using the product type constructor - giving the type
% $(a,b)$.  On the right side, the Haskell expression $(y,x)$ denotes a pair.
% From the left column, we know $x::\sfa$ and $y::\sfb$ so we lift the types up
% into the Cartesian product type $(\sfb,\sfa)$. This yields tthe following
% initial table.

% \begin{center}
% \begin{tabular}{c|l}
% $(x,y)$ & expression \\\hline{}
% $(\sfa,\sfb)$ & $\stackrel{(\sfb,\sfa)}{(y,x)}$\\
% \end{tabular}
% \end{center}

% No further rules apply, and we can read off the type as
% \[swap::(\sfa,\sfb) \rightarrow (\sfb,\sfa)\]

% \subsection{Lambda expressions and function types}

% Consider the following function
% \[g\, (x,y) = \backslash{}f\rightarrow{}f \; y \; x\]

% The inital table for $g$ appears as follows:


% \begin{center}
% \begin{tabular}{cc|l}
% $(x,y)$ & & expression \\\hline{}
% $(\sfa,\sfb)$ & & $\backslash{}f\rightarrow{}((f\;\stackrel{\sfb}{y})\;\stackrel{\sfa}{x})$\\
% \end{tabular}
% \end{center}

% At this step, no rule applies. We need a type for the bound variable $f$ before
% we proceed.  To handle the lambda case we introduce a new rule

% \begin{quotation}
% \noindent{\bf{[Lambda Elimination Rule]}}
% I the expression in the 
% \end{quotation}

% Since the 

% \begin{center}
% \begin{tabular}{cc|l}
% $(x,y)$ & & expression \\\hline{}
% $(\sfa,\sfb)$ & & $\backslash{}f\rightarrow{}f\;\stackrel{\sfb}{y}\;\stackrel{\sfa}{x}$\vspace{.125em}\\\hline\hline
% $(x,y)$ & $f$ & expression\\\hline{}
% $(\sfa,\sfb)$ & $\sfc$ & $\stackrel{c}{f}\;\stackrel{\sfb}{y}\;\stackrel{\sfa}{x}$\\
% \end{tabular}
% \end{center}


\begin{problem}
Use the  table-based method from HW 5 to calculate the types for the following Haskell functions.

\[\begin{array}{ll}
1. & f\; x\; y\; = y\; x \\
2. & {\mathit{curry3}}\;f\;x\;y\;z = f(x,y,z)\\\
3. & {\mathit{uncurry3}} \;f\; (x,y,z) \;= f\;x\;y\;z\\
\end{array}\]


\end{problem}

\end{document}
% Local Variables:
% mode:latex
% comment-column:0
% comment-start: "% "
% compile-command: "pdflatex hw6b"
% fill-column:79
% End:




